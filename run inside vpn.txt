netsh interface ip add address "Loopback Pseudo-Interface 1" 127.0.0.2 255.0.0.0

import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    host: '127.0.0.2',
    port: 5173,
    strictPort: true,
  },
})

route -p add 127.0.0.2 mask 255.255.255.255 0.0.0.0 if 15
